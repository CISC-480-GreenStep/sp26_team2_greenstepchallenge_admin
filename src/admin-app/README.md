# GreenStep Admin App

React application for the GreenStep Sustainability Challenge Admin Console.

Built with **React 19**, **Vite**, **Material UI v7**, **Recharts**, and **React Router v7**.

## Quick Start

```bash
npm install
npm run dev
```

Opens at **http://localhost:5173**. No backend needed — runs entirely with mock data and localStorage.

## Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Start development server with HMR |
| `npm run build` | Production build to `dist/` |
| `npm run preview` | Preview production build locally |
| `npm run lint` | Run ESLint |

## Full Documentation

See the [root README](../../README.md) for project overview, structure, role permissions, data model, version history, and roadmap.
