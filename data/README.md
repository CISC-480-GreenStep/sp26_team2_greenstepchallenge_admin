# Data

Reserved for future data assets (database migrations, seed scripts, schemas, etc.).

Currently, all mock data fixtures live in the app source at `src/admin-app/src/data/mock/`. When the PostgreSQL backend is implemented, this folder will hold migration files and seed data.
